// Demo page with Groq runtime API key UI, model input, and improved adapters section
// (Placeholder: Replace with your actual updated content as needed)

import React from 'react';

export default function Demo() {
  return (
    <div>
      <h1>Garvis Demo (Groq Adapter)</h1>
      {/* Adapter UI, runtime API key input, model selection, etc. */}
      <p>This is the updated demo page with Groq runtime API key UI, model input, and improved adapters section.</p>
    </div>
  );
}
